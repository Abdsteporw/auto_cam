import 'package:flutter/material.dart';

class Drawing_Screen extends StatelessWidget {
  const Drawing_Screen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: GestureDetector(

      ),
    );
  }
}
