import 'package:flutter/material.dart';

class Add_Shelf_Dialog extends StatelessWidget {
  const Add_Shelf_Dialog({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      width: 400,height:500,color: Colors.teal,
    );
  }
}
