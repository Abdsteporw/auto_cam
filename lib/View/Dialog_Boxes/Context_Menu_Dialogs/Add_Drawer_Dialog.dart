import 'package:flutter/material.dart';

class Add_Drawer_Dialog extends StatelessWidget {
  const Add_Drawer_Dialog({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
